<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fakesinsa.fakesinsaboot.mapper.ShopPagingMapper">


    <select id="getProductCnt" parameterType="int" resultType="int">
        select count(product_id) from product;
    </select>

    <select id="listPaging" resultType="hashmap">
        select product_id, product_category, product_name, product_price, product_detail, product_img
        from product
        where product_id > 0
        and product_category = #{category}
        order by product_id
        limit #{pageStart}, #{perPageNum}
    </select>
</mapper>